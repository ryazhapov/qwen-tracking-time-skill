---
description: Show accumulated unattributed time from the usage tracker
---

Show accumulated unattributed time by running:

```
python3 ~/.qwen/skills/qwen-usage-tracker/task_layer.py pending
```

Run that command via your bash tool, then summarize: which branches/repos have unattributed time, and for each, suggest the exact `map` command I should run to attribute it (or tell me to say "skip").
