---
description: Show current task assignments and time-by-task summary
---

Show me the current tracking state by running:

```
python3 ~/.qwen/skills/qwen-usage-tracker/task_layer.py status
```

Run that command via your bash tool, then summarize the output: which tasks have accumulated focus time, how much is unattributed (pending), and whether any branch mappings are missing.
